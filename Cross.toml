[target.aarch64-unknown-linux-gnu]
# Vendor OpenSSL so it builds from source during cross-compilation
# (avoids missing aarch64 dev headers in the cross container)
env.passthrough = ["OPENSSL_STATIC", "OPENSSL_VENDORED"]
