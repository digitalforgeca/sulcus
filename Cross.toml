[target.aarch64-unknown-linux-gnu]
# Install OpenSSL dev headers for aarch64 cross-compilation
pre-build = [
    "dpkg --add-architecture arm64",
    "apt-get update",
    "apt-get install -y libssl-dev:arm64"
]
