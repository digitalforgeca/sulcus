[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sulcus-deepagents"
version = "0.1.0"
description = "Sulcus reactive, thermodynamic memory middleware for LangChain Deep Agents — replace flat AGENTS.md with a real memory engine"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "Digital Forge Studios", email = "contact@sulcus.ca" },
]
keywords = ["sulcus", "deepagents", "langchain", "memory", "agent", "thermodynamic", "vMMU"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "sulcus>=1.0.0",
    "langchain-core>=0.3.0",
    "langchain>=0.3.0",
    "langgraph>=0.2.0",
]

# Note: The DeepAgents framework (deepagents / langchain-agents) is a
# LangChain-based agent system that uses LangGraph under the hood.
# This package integrates with it via langchain-core and langgraph primitives.
# Install the DeepAgents framework separately following its own instructions.

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "mypy>=1.0",
    "ruff>=0.1.0",
]

[project.urls]
Homepage = "https://sulcus.ca"
Repository = "https://github.com/digitalforgeca/sulcus"
Documentation = "https://sulcus.ca/docs"

[tool.hatch.build.targets.wheel]
packages = ["sulcus_deepagents"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.mypy]
python_version = "3.10"
strict = true
ignore_missing_imports = true
