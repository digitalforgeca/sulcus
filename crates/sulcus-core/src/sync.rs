// DELETED: legacy sync / WAL machinery removed. vMMU replaces continuous WAL sync
// with explicit export/import and session-scoped PageTables. Rework the sync layer
// separately if server-side synchronization is required in the new architecture.

// NOTE: this file is intentionally left as a historical stub.