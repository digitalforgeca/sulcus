use anyhow::Result;

pub async fn run(
    _query: &str,
    _limit: u32,
    _memory_type: Option<&str>,
    _min_heat: Option<f64>,
) -> Result<()> {
    eprintln!("sulcus search — not yet wired (Task 2.4)");
    Ok(())
}
